<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Определяем тёмную тему, но с тем же именем Theme.PlaylistMaker -->
    <style name="Theme.PlaylistMaker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Цвета для тёмной темы -->
        <item name="colorPrimary">@color/YP_Black</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/light_gray</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorControlActivated">@color/primary_blue</item>

        <!-- Переопределение базовых цветов для тёмной темы -->
        <item name="colorSurface">@color/YP_Black</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="android:colorBackground">@color/YP_Black</item>
        <item name="colorOnBackground">@color/white</item>

        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="searchIconTint">@color/search_icon_dark</item>
    </style>
</resources>